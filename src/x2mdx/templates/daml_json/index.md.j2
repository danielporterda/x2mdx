{% import "shared/macros.md.j2" as shared %}
{{ heading(1, overview_title) }}

{{ paragraph("This page is generated from versioned Daml docs JSON snapshots.") }}

{{ shared.status_toc(module_status_rows, legend=module_status_legend) }}

{{ shared.version_change_summary(version_change_summary_rows) }}

{{ heading(2, "Reference") }}

{{ bullet_list(module_links) }}

{{ shared.bullet_section("Notes", notes) }}
