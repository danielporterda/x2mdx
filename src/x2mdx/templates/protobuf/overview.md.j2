{% import "shared/macros.md.j2" as shared %}
{{ heading(1, "Canton Protobuf History") }}

{{ paragraph("This page is generated from local descriptor-image snapshots with source info.") }}

{% call shared.standard_reference_layout(toc_rows=toc_rows, version_rows=release_rows) %}
{{ paragraph("Packages are grouped by API area. Each package page contains its services, endpoint history, and shared type reference.") }}
{% for group in package_groups %}

{{ heading(3, group.label) }}

{{ table(["Package", "Services", "Endpoints", "Messages", "Enums"], group.rows) }}
{% endfor %}
{% endcall %}
